<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/C2</item>
        <item name="colorPrimaryDark">@color/C2</item>
        <item name="colorAccent">@color/C3</item>
        <item name="windowNoTitle">true</item>
        <item name="android:colorBackground">@color/C1</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppBarOverlay2" parent="ThemeOverlay.AppCompat.Dark.ActionBar" >
    <item name="android:colorBackground">@color/C4</item>
</style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
<style name="roboto_regular_C2">
    <item name="android:fontFamily">sans-serif</item>
    <item name="android:textColor">@color/C2</item>
</style>
</resources>
